import * as React from 'react';
import { Typography } from '@material-ui/core';


export default class BarGraph extends React.Component {
  render() {
      return <Typography variant='headline' style={{ padding: '20px'}} >Version en desarrollo. Inicia sesión para continuar</Typography>;
  }
}